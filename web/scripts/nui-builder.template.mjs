import { build } from '{{NUI_PATH}}/scripts/build-nui-components.mjs';

/******************************************************
  This script script was auto generated by NUI.
 ******************************************************/

const nuiPath = '{{NUI_PATH}}';
const builderVersion = '{{BUILDER_VERSION}}';
const outDir = import.meta.dirname;

console.log('Started building NUI Components...');
try {
  await build({
    nuiPath,
    target: 'ES2022',
    outDir,
    builderVersion,
  });
}
